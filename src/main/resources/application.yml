spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/product_list}
    username: ${DB_USERNAME:product_app}
    password: ${DB_PASSWORD:product_app}
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml

